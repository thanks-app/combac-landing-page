---
import CookiesPolicyPage from "@modules/cookiesPolicy";
import Layout from "Layout.astro";
import { getTemplateConfig } from "utils/config";

const lang = "ar";
const config = getTemplateConfig(lang);
---

<Layout
  {...config}
  seo={config.cookiesPolicy?.seo}
>
  <CookiesPolicyPage client:load config={config} />
</Layout>
