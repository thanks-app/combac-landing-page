---
import AppPage from "@modules/app"
import Layout from "Layout.astro"
import { getTemplateConfig } from "utils/config"

const lang = "fr";
const config = getTemplateConfig(lang);
---

<Layout {...config}>
  <AppPage client:load config={config} />
</Layout>
