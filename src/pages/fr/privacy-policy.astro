---
import PrivacyPolicyPage from "@modules/privacyPolicy";
import Layout from "Layout.astro";
import { getTemplateConfig } from "utils/config";

const lang = "fr";
const config = getTemplateConfig(lang);
---

<Layout
  {...config}
  seo={config.privacyPolicy?.seo}
>
  <PrivacyPolicyPage client:load config={config} />
</Layout>
